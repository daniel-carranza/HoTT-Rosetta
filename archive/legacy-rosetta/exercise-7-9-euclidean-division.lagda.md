# Exercise 7.9 Euclidean division

```agda
module exercise-7-9-euclidean-division where
```

## Problem statement

Consider two natural numbers `a` and `b`.
Construct two natural numbers `q` and `r` such that `(b ≠ 0) → (r < b)`, along
with an identification

```text
  a = qb + r.
```

## Solution

## Problem statement

Show that for any four natural numbers `q, q'` and `r, r'` such that the
implications `(b ≠ 0) → (r < b)` and `(b ≠ 0) → (r' < b)` hold, and for which
there are identifications

```text
  a = qb + r
  a = q'b + r',
```

we have `q = q'` and `r = r'`.

## Solution
