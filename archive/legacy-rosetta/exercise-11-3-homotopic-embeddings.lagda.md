# Exercise 11.3 Homotopic embeddings

```agda
module exercise-11-3-homotopic-embeddings where
```

## Problem statement

Show that

```text
  (f ~ g) → (is-emb(f) ↔ is-emb(g))
```

for any `f, g : A → B`.

## Solution
