# Exercise 13.4 Equivalence structure is a proposition

```agda
module exercise-13-4-equivalence-structure-is-a-proposition where
```

## Problem statement

Let `f : A → B` be a function.
Show that if `f` is an equivalence, then the type

```text
  Σ(g : B → A) f ∘ g ~ id
```

of sections of `f` is contractible.

## Solution

## Problem statement

Show that if `f` is an equivalence, then the type

```text
  Σ(h : B → A) h ∘ f ~ id
```

of retractions of `f` is contractible.

## Solution

## Problem statement

Show that `is-equiv(f)` is a proposition.

## Solution

## Problem statement

Show that for any two equivalences `e e' : A ≃ B`, the canonical map

```text
  (e = e') → (e ~ e')
```

is an equivalence.

## Solution

## Problem statement

Show that the type `A ≃ B` is a `k`-type if both `A` and `B` are `k`-types.

## Solution
