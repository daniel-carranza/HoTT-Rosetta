# Exercise 15.4 Surjective triangles

```agda
module exercise-15-4-surjective-triangles where
```

## Problem statement

Consider a commuting triangle

```text
      h
  A ----> B
   \    /
  f \  / g
     X
```

with `H : f ~ g ∘ h`.
Show that if `f` is surjective, then `g` is surjective.

## Solution

## Problem statement

Show that if both `g` and `h` are surjective, then `f` is surjective.

## Solution

## Problem statement

As a converse to Exercise `ex:is-trunc-comp`, show that if `f` and `h` are
`k`-truncated, then `g` is also `k`-truncated.

## Solution
