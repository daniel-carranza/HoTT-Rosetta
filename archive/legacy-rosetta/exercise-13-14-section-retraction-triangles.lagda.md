# Exercise 13.14 Section retraction triangles

```agda
module exercise-13-14-section-retraction-triangles where
```

## Problem statement

Consider a commuting triangle

```text
      h
  A ----> B
   \    /
  f \  / g
     X
```

with `H : f ~ g ∘ h`.
Show that if `h` has a section, then `sections(g)` is a retract of
`sections(f)`.

## Solution

## Problem statement

Show that if `g` has a retraction, then `retractions(h)` is a retract of
`sections(f)`.

## Solution
