# Exercise 17.1 Universes of truncated types

```agda
module exercise-17-1-universes-of-truncated-types where
```

## Problem statement

Use the univalence axiom to show that the type
`Σ(A : 𝒰) is-contr(A)` of all contractible types in `𝒰` is contractible.

## Solution

## Problem statement

Use the univalence axiom and Exercises `ex:isprop_istrunc` and
`ex:isprop_isequiv` to show that the universe of `k`-types

```text
  𝒰^{≤ k} := Σ(X : 𝒰) is-trunc(k, X)
```

is a `(k + 1)`-type, for any `k ≥ -2`.

## Solution

## Problem statement

Show that `Prop_𝒰` is not a proposition.

## Solution

## Problem statement

Show that the universe `Set_𝒰` of sets in `𝒰` is not a set.

## Solution
