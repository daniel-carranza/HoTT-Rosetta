# Exercise 16.2 Retracts of counted and finite types

```agda
module exercise-16-2-retracts-of-counted-and-finite-types where
```

## Problem statement

Suppose that `A` is a retract of `B`.
Show that `count(B) → count(A)`.
Conclude that `is-finite(B) → is-finite(A)`.

## Solution
