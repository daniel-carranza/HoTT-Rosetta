# Exercise 17.19 Embeddings between uniform finite types

```agda
module exercise-17-19-embeddings-between-uniform-finite-types where
```

## Problem statement

Show that for `k ≥ 3`, the type

```text
  Π(X : BS_k) (X + X) ↪ (X × X) + unit
```

is empty, even though the inequality `2k ≤ k² + 1` holds for all `k : ℕ`.

## Solution

## Problem statement

Show that the type

```text
  Π(X : BS_2) (X + X) ↪ (X × X) + unit
```

is equivalent to `Fin(8)`.

## Solution
