# Exercise 11.7 Coproduct functor reflection

```agda
module exercise-11-7-coproduct-functor-reflection where
```

## Problem statement

Consider two maps `f : A → A'` and `g : B → B'`.
Show that if the map

```text
  f + g : (A + B) → (A' + B')
```

is an equivalence, then so are both `f` and `g`.
This is the converse of Exercise 9.6.

## Solution

## Problem statement

Show that `f + g` is an embedding if and only if both `f` and `g` are
embeddings.

## Solution
