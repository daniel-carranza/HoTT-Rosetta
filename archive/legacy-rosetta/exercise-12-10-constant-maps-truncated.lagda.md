# Exercise 12.10 Constant maps truncated

```agda
module exercise-12-10-constant-maps-truncated where
```

## Problem statement

Show that a type `A` is a `(k + 1)`-type if and only if the map
`const_x : unit → A` is `k`-truncated for every `x : A`.

## Solution
