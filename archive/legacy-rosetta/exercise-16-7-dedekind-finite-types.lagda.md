# Exercise 16.7 Dedekind finite types

```agda
module exercise-16-7-dedekind-finite-types where
```

## Problem statement

Consider a finite type `X`.
Show that any embedding `f : X → X` is an equivalence.
Sets `X` such that every embedding `X ↪ X` is an equivalence are also called
**Dedekind finite**.

## Solution

## Problem statement

Show that any surjective map `f : X → X` is an equivalence.

## Solution
