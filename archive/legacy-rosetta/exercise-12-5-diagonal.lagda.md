# Exercise 12.5 Diagonal

```agda
module exercise-12-5-diagonal where
```

## Problem statement

Let `A` be a type, and let the **diagonal** of `A` be the map
`δ_A : A → A × A` given by `λ x → (x, x)`.
Show that

```text
  is-equiv(δ_A) ↔ is-prop(A).
```

## Solution

## Problem statement

Construct an equivalence `fib(δ_A, (x, y)) ≃ (x = y)` for any `x y : A`.

## Solution

## Problem statement

Show that `A` is `(k + 1)`-truncated if and only if
`δ_A : A → A × A` is `k`-truncated.

## Solution
