# Exercise 9.2 Nonequivalences of booleans and natural numbers

```agda
module exercise-9-2-nonequivalences-booleans-natural-numbers where
```

## Problem statement

Use Exercise 6.2 to show that the constant function
`const_b : bool → bool` is not an equivalence, for any `b : bool`.

## Solution

## Problem statement

Show that `bool` is not equivalent to `𝟙`.

## Solution

## Problem statement

Show that `ℕ` is not equivalent to `Fin(k)` for any `k : ℕ`.

## Solution
