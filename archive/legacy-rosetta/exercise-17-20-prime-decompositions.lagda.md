# Exercise 17.20 Prime decompositions

```agda
module exercise-17-20-prime-decompositions where
```

## Problem statement

For any natural number `n` consider the type

```text
  D~_n := Σ(X : BS_2) Σ(Y : X → 𝔽) (Fin(n) ≃ Π(x : X) Y(x)).
```

Show that `D~_1 ≃ BS_2`.

## Solution

## Problem statement

Show that `D~_n` is contractible if and only if `n` is prime.

## Solution

## Problem statement

Show that `D~_n` is a set if and only if `n` is not a square.

## Solution
