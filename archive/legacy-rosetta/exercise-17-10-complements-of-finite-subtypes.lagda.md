# Exercise 17.10 Complements of finite subtypes

```agda
module exercise-17-10-complements-of-finite-subtypes where
```

## Problem statement

For any `k : ℕ`, show that the type

```text
  Σ(X : BS_{k + 1}) Fin(k) ↪ X
```

is contractible.

## Solution

## Problem statement

More generally, construct for any `k l : ℕ` and any `k`-element type `A` an
equivalence

```text
  (Σ(X : BS_{k + l}) A ↪ X) ≃ BS_l.
```

## Solution
