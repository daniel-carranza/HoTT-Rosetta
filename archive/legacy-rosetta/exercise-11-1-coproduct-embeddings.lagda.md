# Exercise 11.1 Coproduct embeddings

```agda
module exercise-11-1-coproduct-embeddings where
```

## Problem statement

Show that the map `∅ → A` is an embedding for every type `A`.

## Solution

## Problem statement

Show that `inl : A → A + B` and `inr : B → A + B` are embeddings for any two
types `A` and `B`.

## Solution

## Problem statement

Show that `inl : A → A + B` is an equivalence if and only if `B` is empty, and
that `inr : B → A + B` is an equivalence if and only if `A` is empty.

## Solution
