# Exercise 17.15 Small retracts and products

```agda
module exercise-17-15-small-retracts-and-products where
```

## Problem statement

Let `𝒰` be a univalent universe.
Consider a section-retraction pair

```text
       i       r
  A ------> X -----> A
```

with `H : r ∘ i ~ id`.
Show that if `X` is `𝒰`-small, then so is `A`.
Hint: Use Exercise `ex:retracts-as-limits`.

## Solution

## Problem statement

Consider two inhabited types `A` and `B`.
Show that if the product `A × B` is `𝒰`-small, then so are the types `A` and
`B`.

## Solution
