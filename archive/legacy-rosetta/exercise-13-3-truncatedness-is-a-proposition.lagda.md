# Exercise 13.3 Truncatedness is a proposition

```agda
module exercise-13-3-truncatedness-is-a-proposition where
```

## Problem statement

Show that for any type `A` the type `is-contr(A)` is a proposition.

## Solution

## Problem statement

Show that for any type `A` and any `k ≥ -2`, the type `is-trunc(k, A)` is a
proposition.

## Solution
