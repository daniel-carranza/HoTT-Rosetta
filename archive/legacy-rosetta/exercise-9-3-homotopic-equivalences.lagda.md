# Exercise 9.3 Homotopic equivalences

```agda
module exercise-9-3-homotopic-equivalences where
```

## Problem statement

Consider two functions `f, g : A → B` and a homotopy `H : f ~ g`.
Then

```text
  is-equiv(f) ↔ is-equiv(g).
```

## Solution

## Problem statement

Show that for any two homotopic equivalences `e, e' : A ≃ B`, their inverses are
also homotopic.

## Solution
