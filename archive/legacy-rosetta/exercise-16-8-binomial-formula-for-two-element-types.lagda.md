# Exercise 16.8 Binomial formula for two-element types

```agda
module exercise-16-8-binomial-formula-for-two-element-types where
```

## Problem statement

Consider two arbitrary types `A` and `B`.
For any `2`-element type `X`, construct an equivalence

```text
  (A + B)^X ≃ A^X + X × (A × B) + B^X.
```

## Solution
