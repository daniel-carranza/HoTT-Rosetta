# Exercise 12.4 Coproduct truncation

```agda
module exercise-12-4-coproduct-truncation where
```

## Problem statement

Show that for any two contractible types `A` and `B`, the coproduct `A + B` is
not contractible.

## Solution

## Problem statement

Show that for any two propositions `P` and `Q`, we have a logical equivalence

```text
  is-contr(P + Q) ↔ P ⊕ Q,
```

where the **exclusive disjunction** `P ⊕ Q` is defined by

```text
  P ⊕ Q := (P × ¬Q) + (Q × ¬P).
```

## Solution

## Problem statement

Show that for any two propositions `P` and `Q`, the coproduct `P + Q` is a
proposition if and only if `P → ¬Q`.

## Solution

## Problem statement

Show that for any two `(k + 2)`-types `A` and `B`, the coproduct `A + B` is
again a `(k + 2)`-type.
Conclude that `ℤ` is a set.

## Solution
