# Exercise 8.9 Finite dependent products

```agda
module exercise-8-9-finite-dependent-products where
```

## Problem statement

Consider a family `B` of types over `Fin(k)`, for some `k : ℕ`.
Show that if each `B(x)` is decidable, then `Π(x : Fin(k)) B(x)` is again
decidable.

## Solution

## Problem statement

Show that if each `B(x)` has decidable equality, then
`Π(x : Fin(k)) B(x)` also has decidable equality.

## Solution
