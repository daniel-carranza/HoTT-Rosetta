# Exercise 12.8 Retracts of truncated types

```agda
module exercise-12-8-retracts-of-truncated-types where
```

## Problem statement

Consider a section-retraction pair

```text
       i       r
  A ------> B -----> A
```

with `H : r ∘ i ~ id`.
Show that `x = y` is a retract of `i(x) = i(y)`.

## Solution

## Problem statement

Use Exercise `ex:contr_retr` to show that if `A` is a retract of a `k`-type
`B`, then `A` is also a `k`-type.

## Solution
