# Exercise 17.6 Identity type embedding

```agda
module exercise-17-6-identity-type-embedding where
```

## Problem statement

Consider a type `A` in `𝒰`.
Show that the identity type, seen as a function

```text
  Id_A : A → (A → 𝒰),
```

is an embedding.

## Solution
