# Exercise 8.4 Prime and prime-counting functions

```agda
module exercise-8-4-prime-and-prime-counting-functions where
```

## Problem statement

Define the **prime function** `prime : ℕ → ℕ` for which `prime(n)` is the `n`-th
prime.

## Solution

## Problem statement

Define the **prime-counting function** `π : ℕ → ℕ`, which counts for each
`n : ℕ` the number of primes `p ≤ n`.

## Solution
