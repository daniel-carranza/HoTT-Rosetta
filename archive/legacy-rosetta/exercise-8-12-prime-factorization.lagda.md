# Exercise 8.12 Prime factorization

```agda
module exercise-8-12-prime-factorization where
```

## Problem statement

Show that every natural number `n ≥ 2` has a prime factor.

## Solution

## Problem statement

Define a function

```text
  prime-factors : (Σ(n : ℕ) 2 ≤ n) → list(ℕ)
```

such that `prime-factors(n)` is an increasing list of primes, and `n` is the
product of the primes in the list `prime-factors(n)`.

## Solution

## Problem statement

Show that any increasing list `l` of primes of which the product is `n` is equal
to the list `prime-factors(n)`.

## Solution
