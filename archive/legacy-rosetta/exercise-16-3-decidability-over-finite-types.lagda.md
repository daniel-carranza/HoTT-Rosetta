# Exercise 16.3 Decidability over finite types

```agda
module exercise-16-3-decidability-over-finite-types where
```

## Problem statement

Consider a family of decidable types `A_i` indexed by a finite type `I`.
Show that the dependent product

```text
  Π(i : I) A_i
```

is decidable.

## Solution

## Problem statement

Show that `is-emb(f)` is decidable, for any map `f : I → J` between finite
types.

## Solution

## Problem statement

Show that `is-surj(f)` is decidable, for any map `f : I → J` between finite
types.

## Solution

## Problem statement

Show that `is-equiv(f)` is decidable, for any map `f : I → J` between finite
types.

## Solution
