# Exercise 10.3 Contractible types and equivalences

```agda
module exercise-10-3-contractible-equivalences where
```

## Problem statement

Show that for any type `A`, the map `const_⋆ : A → 𝟙` is an equivalence if and
only if `A` is contractible.

## Solution

## Problem statement

Apply Exercise 9.4 to show that for any map `f : A → B`, if any two of the
three assertions

1. `A` is contractible,
2. `B` is contractible,
3. `f` is an equivalence,

hold, then so does the third.

## Solution
