# Exercise 18.8

```agda
module exercise-18-8-exercise where

```

## Problem statement

Consider a type `A`, and suppose that `‖A‖_0` is a finite type with `k` elements.
Show that there exists a map `f:Fin{k}→ A` such that `η∘ f` is an equivalence, i.e., prove the proposition
```text
∃_{(f:Fin{k}→ A)}is-equiv(η∘ f).
```

## Solution

<!-- rosetta-item: exercise-18-8 -->

No formalization has been curated yet.
