# Exercise 17.15

```agda
module exercise-17-15-small-retracts-and-products where

```

## Problem statement

Let `𝒰` be a univalent universe.

<div class="subexenum">

Consider a section-retraction pair
<!-- rosetta-diagram: dff8c57ba552; review: pending -->

*Linear diagram (automatic draft).*

```text
 [A] ----> [X] ----> [A]

Arrows:
- A --i--> X
- X --r--> A
```
with `H:r∘ i~ id`.
Show that if `X` is `𝒰`-small, then so is `A`.
Hint: Use Exercise 13.18.

Consider two inhabited types `A` and `B`.
Show that if the product `A× B` is `𝒰`-small, then so are the types `A` and `B`.

</div>

## Solution

<!-- rosetta-item: exercise-17-15 -->

No formalization has been curated yet.
