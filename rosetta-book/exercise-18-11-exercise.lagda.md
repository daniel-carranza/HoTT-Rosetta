# Exercise 18.11

```agda
module exercise-18-11-exercise where

```

## Problem statement

Consider two universes `𝒰` and `𝒱`.
Use the type theoretic replacement axiom to show that the type of locally `𝒱`-small types in `𝒰` is equivalent to the type
```text
\sum_{(Y:Locally-𝒱-Small-Set_𝒰)}(Y→ Σ(Z:𝒱) is-conn(Z))
```
of locally `𝒱`-small sets `Y` in `𝒰` equipped with a family of connected types in `𝒱`.

## Solution

<!-- rosetta-item: exercise-18-11 -->

No formalization has been curated yet.
