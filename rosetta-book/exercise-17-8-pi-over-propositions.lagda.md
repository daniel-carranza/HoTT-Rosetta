# Exercise 17.8

```agda
module exercise-17-8-pi-over-propositions where

```

## Problem statement

(Escardó) Consider a proposition `P` and a universe `𝒰` containing `P`.
Show that the map
```text
Π_P : (P→ 𝒰)→𝒰,
```
given by `A↦Π(p:P) A(p)`, is an embedding.

## Solution

<!-- rosetta-item: exercise-17-8 -->

No formalization has been curated yet.
