# Exercise 8.11

```agda
module exercise-8-11-bezouts-identity where

```

## Problem statement

<div class="subexenum">

For any three natural numbers `x`, `y`, and `z`, show that the type
```text
Σ(k:ℕ) Σ(l:ℕ) dist-ℕ(kx,ly)=z
```
is decidable.

(Bézout’s identity) For any two natural numbers `x` and `y`, construct two natural numbers `k` and `l` equipped with an identification
```text
dist-ℕ(kx,ly)=gcd(x,y).
```

</div>

## Solution

<!-- rosetta-item: exercise-8-11 -->

No formalization has been curated yet.
