# Exercise 18.3

```agda
module exercise-18-3-exercise where

```

## Problem statement

For any type `A`, show that the type of equivalence relations equipped with a choice of representatives of its equivalence classes is equivalent to the type of **set-based retracts** of `A`, i.e., the type
```text
Retr_{Set_𝒰}(A) ≔ Σ(X:Set_𝒰) Σ(i:X→ A) Σ(q:A→ X) q∘ i~ id.
```

## Solution

<!-- rosetta-item: exercise-18-3 -->

No formalization has been curated yet.
