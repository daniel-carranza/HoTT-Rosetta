# Exercise 17.7

```agda
module exercise-17-7-sigma-and-coproduct-truncated-maps where

```

## Problem statement

<div class="subexenum">

For any type `A` in `𝒰`, consider the function
```text
Σ_A : (A → 𝒰) → 𝒰,
```
which takes a family `B` of `𝒰`-small types to its `Σ`-type.
Show that the following are equivalent:

1.  The type `A` is `k`-truncated.

2.  The map `Σ_A` is `k`-truncated.

Hint: Construct an equivalence `fib(Σ_A, X)≃ (X→ A)`.

Show that the map `{+}:𝒰×𝒰→𝒰`, which takes `(A,B)` to the coproduct `A+B`, is `0`-truncated.

</div>

## Solution

<!-- rosetta-item: exercise-17-7 -->

No formalization has been curated yet.
