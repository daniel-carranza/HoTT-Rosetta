# Exercise 13.16

```agda
module exercise-13-16-equivalences-and-isomorphisms-of-sets where

```

## Problem statement

Let `A` and `B` be sets.
Show that type `A ≃ B` of equivalences from `A` to `B` is equivalent to the type `A≅ B` of **isomorphisms** from `A` to `B`, i.e., the type of quadruples `(f,g,H,K)` consisting of
```text
f : A→ B
g : B→ A
H : f∘ g = id[B]
K : g∘ f = id[A].
```

## Solution

<!-- rosetta-item: exercise-13-16 -->

No formalization has been curated yet.
