# Exercise 19.11

```agda
module exercise-19-11-exercise where

```

## Problem statement

Consider a commutative monoid `M`.
Define an operation
```text
Π(X:\mathbb{F}) M^X→ M
```
that extends the (binary) monoid operation to the finite unordered `n`-tuples of elements in `M`.

## Solution

<!-- rosetta-item: exercise-19-11 -->

No formalization has been curated yet.
