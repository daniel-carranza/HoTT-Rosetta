# Exercise 17.12

```agda
module exercise-17-12-automorphisms-of-k-element-types where

```

## Problem statement

<div class="subexenum">

Show that for `k≠ 2`, the type
```text
Π(X:BS_k) X→ X
```
is contractible.
Conclude that the type `Π(X:BS_k) X≃ X` is also contractible.
Hint: Use Exercise 17.11.

Show that the type
```text
Π(X:BS_2) X→ X
```
is equivalent to `Fin{2}`.
Conclude that the type `Π(X:BS_2) X≃ X` is also equivalent to `Fin{2}`.

</div>

## Solution

<!-- rosetta-item: exercise-17-12 -->

No formalization has been curated yet.
