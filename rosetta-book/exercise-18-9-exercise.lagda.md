# Exercise 18.9

```agda
module exercise-18-9-exercise where

```

## Problem statement

Consider a type `A` and a universe `𝒰` containing `A`.
Let
```text
τ:A→ ((A→Prop_𝒰)→Prop_𝒰)
```
be the map defined by `τ(a)≔λ f. f(a)`.
Show that the map
```text
q_τ : A→im(τ)
```
obtained from the image factorization of `A` is a set truncation of `A`.

## Solution

<!-- rosetta-item: exercise-18-9 -->

No formalization has been curated yet.
