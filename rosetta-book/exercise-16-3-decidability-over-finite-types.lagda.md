# Exercise 16.3

```agda
module exercise-16-3-decidability-over-finite-types where

```

## Problem statement

<div class="subexenum">

Consider a family of decidable types `A_i` indexed by a finite type `I`.
Show that the dependent product
```text
Π(i:I) A_i
```
is decidable.

Show that `is-emb(f)` is decidable, for any map `f:I→ J` between finite types.

Show that `is-surj(f)` is decidable, for any map `f:I→ J` between finite types.

Show that `is-equiv(f)` is decidable, for any map `f:I→ J` between finite types.

</div>

## Solution

<!-- rosetta-item: exercise-16-3 -->

No formalization has been curated yet.
