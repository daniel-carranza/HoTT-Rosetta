# Exercise 18.2

```agda
module exercise-18-2-exercise where

```

## Problem statement

Consider an equivalence relation `R:A→(A→Prop_𝒰)` on `A`, where `𝒰` is a universe containing `A`.
Show that type type
```text
Σ(X:𝒰) Σ(f:A↠ X) Π(x,y:A) (f(x)=f(y))≃ R(x,y)
```
is contractible.

## Solution

<!-- rosetta-item: exercise-18-2 -->

No formalization has been curated yet.
