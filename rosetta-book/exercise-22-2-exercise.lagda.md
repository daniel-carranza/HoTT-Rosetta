# Exercise 22.2

```agda
module exercise-22-2-exercise where

```

## Problem statement

<div class="subexenum">

Show that for every `x:X`, we have an equivalence
```text
(Σ(f:S^1→ X) f(base)= x ) ≃ (x=x)
```

Show that for every `t:S^1`, we have an equivalence
```text
(Σ(f:S^1→ S^1) f(base)= t ) ≃ ℤ
```
The base point preserving map `f:S^1→S^1` corresponding to `k:ℤ` is the degree `k` map on the circle.

Show that for every `t:S^1`, we have an equivalence
```text
(Σ(e:S^1 ≃ S^1) e(base)= t ) ≃ bool
```

</div>

## Solution

<!-- rosetta-item: exercise-22-2 -->

No formalization has been curated yet.
