# Exercise 20.3

```agda
module exercise-20-3-exercise where

```

## Problem statement

For a type family `B` over `A`, suppose that each `B(x)` is empty.
Show that the type `W(A,B)` is equivalent to the type `A`.

## Solution

<!-- rosetta-item: exercise-20-3 -->

No formalization has been curated yet.
