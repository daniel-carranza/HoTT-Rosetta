# Exercise 21.4

```agda
module exercise-21-4-exercise where

```

## Problem statement

<div class="subexenum">

Show that a type `X` is a set if and only if the map
```text
λ x. λ t. x : X → (S^1→ X)
```
is an equivalence.

Show that a type `X` is a set if and only if the map
```text
λ f. f(base) : (S^1→ X)→ X
```
is an equivalence.

</div>

## Solution

<!-- rosetta-item: exercise-21-4 -->

No formalization has been curated yet.
