# Exercise 12.5

```agda
module exercise-12-5-diagonal where

```

## Problem statement

Let `A` be a type, and let the **diagonal** of `A` be the map `\delta_A:A→ A× A` given by `λ x. (x,x)`.

<div class="subexenum">

Show that
```text
{is-equiv(\delta_A)}↔{is-prop(A)}.
```

Construct an equivalence `fib(\delta_A, x,y) ≃ (x=y)` for any `x,y:A`.

Show that `A` is `(k+1)`-truncated if and only if `\delta_A:A→ A× A` is `k`-truncated.

</div>

## Solution

<!-- rosetta-item: exercise-12-5 -->

No formalization has been curated yet.
