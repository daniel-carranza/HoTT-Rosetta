# Exercise 20.4

```agda
module exercise-20-4-exercise where

```

## Problem statement

<div class="subexenum">

Show that the elementhood relation `{∈}` on `W(A,B)` is irreflexive, for any type family `B` over any type `A`.

Use the previous fact along with Proposition 20.6.7 to give a second proof of the fact that there can be no type `U:𝒰` equipped with an equivalence `𝒰≃ U`.

</div>

## Solution

<!-- rosetta-item: exercise-20-4 -->

No formalization has been curated yet.
